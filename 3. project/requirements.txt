# --- TensorFlow (platform-aware) ---
tensorflow-macos==2.15.0 ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal==1.1.0   ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow==2.15.0        ; not (platform_system == "Darwin" and platform_machine == "arm64")

# --- Core scientific stack ---
numpy==1.26.4
matplotlib==3.8.4
pillow==10.3.0

# --- For model diagram via keras.utils.plot_model ---
pydot==1.4.2
graphviz==0.20.1
# NOTE: OS-level Graphviz (dot) is also needed:
# macOS:  brew install graphviz
# Ubuntu/Debian:  sudo apt-get install graphviz
# Windows:  choco install graphviz
